import 'package:flutter/cupertino.dart';

class Giftspagescreen extends StatelessWidget {
  const Giftspagescreen({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Gift"));
  }
}
