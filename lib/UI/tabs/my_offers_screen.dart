import 'package:flutter/cupertino.dart';

// ignore: camel_case_types
class myofferesscreen extends StatelessWidget {
  const myofferesscreen({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("My Offers"));
  }
}
