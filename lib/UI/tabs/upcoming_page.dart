import 'package:flutter/cupertino.dart';

class Upcomingpagecreen extends StatelessWidget {
  const Upcomingpagecreen({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Upcoming"));
  }
}
